/**
 * Bump when `templates.json` / `moods.json` / `tarot_cards.json` / generator wording changes so
 * cached `tarot_daily` rows are regenerated (GET repair + prewarm).
 */
export const TAROT_COPY_REV = 20;
