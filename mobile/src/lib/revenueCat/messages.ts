/** Shown when EXPO_PUBLIC_REVENUECAT_API_KEY_IOS is unset (Apple setup pending). */
export const PURCHASES_NOT_CONFIGURED_MESSAGE =
  'Purchases are not configured yet. Add RevenueCat API keys and package identifiers in mobile/.env, then test on a real device.';
